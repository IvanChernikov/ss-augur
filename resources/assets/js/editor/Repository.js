let Repository = function () {
    this.data = new Map();
    this.types = new Map();
};

/**
 *
 * @param {Array} types
 */
Repository.prototype.init = function (types) {
    for (let i in types) {
        this.data
    }
};

/**
 * Loads a
 * @param type
 * @param {Entity} entity
 */
Repository.prototype.load = function (type, entity) {
    if (this.data.has(entity.id)) {

    }
};